import css from "./Button.module.css";

const Button = () => {
    return (
        <button type="button" className={css.button}>
            Follow
        </button>
    );
};

export default Button;
